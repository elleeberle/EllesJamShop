{
  "name": "elles-jam-shop",
  "version": "1.0.0",
  "description": "Jam order form",
  "scripts": {
    "build": "esbuild form.jsx --bundle --outfile=dist/app.js --loader:.jsx=jsx"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
